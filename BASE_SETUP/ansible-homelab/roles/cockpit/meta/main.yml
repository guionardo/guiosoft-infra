---
galaxy_info:
  author: guionardo
  description: Installs Cockpit web console with plugins (sensors, dockermanager, navigator, benchmark, compose, cloudflared, file-sharing)
  license: MIT
  min_ansible_version: '2.14'
  platforms:
    - name: Debian
      versions:
        - trixie
        - bookworm
dependencies: []

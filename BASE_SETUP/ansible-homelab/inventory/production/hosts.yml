---
all:
  children:
    homelab:
      hosts:
        guiosoft-info:
          ansible_host: 192.168.88.9
          ansible_user: guionardo
          ansible_ssh_private_key_file: ~/.ssh/id_rsa
           # ansible_become_pass removed for security — use --ask-become-pass
